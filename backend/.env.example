# Azure OpenAI Credentials
# Rename this file to .env and fill in your credentials

# Your Azure OpenAI API key
AZURE_OPENAI_API_KEY=your_api_key_here

# The Azure OpenAI endpoint URL
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# IMPORTANT: This should be the DEPLOYMENT name, not the model name
# In the Azure OpenAI Studio, you create a deployment of a model, and give that deployment a name
# Example: If you deployed the "gpt-4o-mini" model and named the deployment "my-gpt4-deployment",
# you should use "my-gpt4-deployment" here, NOT "gpt-4o-mini"
AZURE_OPENAI_DEPLOYMENT_NAME=your_deployment_name

# Additional configuration options
# Uncomment the following lines if needed

# Maximum tokens to generate in each response
# MAX_TOKENS=800

# Temperature setting for response generation (0.0 to 1.0)
# TEMPERATURE=0.7 